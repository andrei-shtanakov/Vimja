Add the 'o' functionality: self.editor.insert_new_line() followed by a change to insert mode

Change the keyMap json indices to be the Qt values as opposed to the hard coded versions

Create some kind of decorator for the yank and delete modes

Stop the buffer from including events that aren't meant to be captured by NINJA-IDE

Try creating an instance variable for the cursor instead of getting it every time I need it

Create exception handling
